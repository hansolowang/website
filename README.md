Generate html with `console.log(document.getElementsByTagName('html')[0].innerHTML);` in the Console  
add <html> to top  
add </html> to bottom

## Local development

```
hugo server
```

Runs the site at http://localhost:1313.
